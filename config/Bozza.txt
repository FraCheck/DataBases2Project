Website:
	-> RegistrationPage
		Content:
			-> 3 Textboxes (user,pass,email)
			-> 1 Button to send the registration
		Functionality:
			-> Redirect to Homepage if registration succeed
			
	-> LoginForm
		Content:
			-> 2 Textboxes (user/email,pass)
			-> 1 Button to send the login
		Functionality:
			-> Redirect to Homepage if login succeed
			
	-> HomePage
		Content:
			-> Name and image of the "product of the day"
			-> Product reviews by other users
		Links:
			-> "Questionnaire of the day" --> QuestionairePage
			
	-> QuestionnairePage
		Content:
			- Section1:
				Content:
					-> Variable numbers of question labels + textboxes answers
					-> 1 Button "Next" to access Section2
				Constraints : All marketing inputs are mandatory in order to submit.
			- Section2:
				Content:
					-> Fixed numbers of inputs elements for age,sex,expertise level
					-> 2 Button "Submit", "Cancel", "Previous"
				Constraints: All statistical inputs are optional
			Functionality:
				-> Redirec to Thanks&GreetingsPage on "Submit"
	-Thanks&GreetingsPage
	
Tables:
	USER:
		-> int id PRIMARY KEY
		-> String username NOT NULL UNIQUE
		-> String password NOT NULL
		-> String email NOT NULL UNIQUE
		-> bool banned NOT NULL					// = 1 se utente è bannato, 0 altrimenti
		-> ? Orario di accesso, non sono sicuro di come possa funzionare
		-> int role ?
	PRODUCT:
		-> int id PRIMARY KEY
		-> String name NOT NULL UNIQUE
		-> byte() thumb mediumBlob
		-> byte() image longBlob
		-> String caption
		
	REVIEWS:
		-> int id PRIMARY KEY
		-> int product_id FOREIGN KEY ON UPDATE RESTRICT, ON DELETE RESTRICT
		-> string review_text

	QUESTIONNAIRE:
		-> int id PRIMARY KEY
		-> date date NOT NULL UNIQUE
		-> int product NOT NULL, FOREIGN KEY
		
	MARKETING_QUESTIONS:
		-> int id PRIMARY KEY
		-> String question NOT NULL UNIQUE
		-> int questionnaire_id FOREIGN KEY
		
	QUESTIONNAIRE_USER_ANSWERS
		-> int id PRIMARY KEY
		-> int age
		-> char sex
		-> int expertise_level
		-> bool cancelled
		-> int questionnaire_id FOREIGN KEY 
		-> int user FOREIGN KEY
		
	MARKETING_ANSWERS:
		-> int id PRIMARY KEY
		-> String answer
		-> int questionnaire_user_answers FOREIGN KEY
		
	BAD_WORDS:
		-> int id PRIMARY KEY
		-> String word
		